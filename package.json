{
  "name": "create-visual-article-site",
  "version": "2.1.2",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test scripts/tests/*.test.mjs",
    "validate": "npm test"
  }
}
